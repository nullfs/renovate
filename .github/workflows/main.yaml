name: CI/CD
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
jobs:
  main:
    uses: ./.github/workflows/docker.yaml
    with:
      image_repository: docker.io/library/alpine
      image_tag: 3.21.3
